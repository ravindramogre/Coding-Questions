#include<bits/stdc++.h>
using namespace std;
void init_code()
{
    #ifndef ONLINE_JUDGE
freopen("input.txt", "r", stdin);
freopen("output.txt", "w", stdout);
#endif // ONLINE_JUDGE
}

int main()
{	
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	init_code();
	string s;
	cin>>s;
	vector <string> temp;
	//skip "WUB" in the string and then,
	//store remaining sub-strings in a array of strings
	return 0;
}